1.简而言之，web前端需要学HTML5、CSS3、JavaScript、JQuery等核心内容，将这些核心知识
学到极致，同时全面覆盖响应式布局、Bootstrap、AngularJS、React、NodeJS等当前互联网开发的
最新技术。我花了4个月在泛艺学苑学完了这些内容，差不多入门了，后面还需要深入研究。



2.学习计划：十月份要将git和c整本书学完，接着开始学习python，
期间夹杂着HTML，javascript，PHP最重要！！！

3.Git是目前世界上最先进的 分布式 版本控制系统（没有之一）。

4.首先，你可以试着输入git，看看系统有没有安装Git：
$ git
The program 'git' is currently not installed. You can install it by typing:
sudo apt-get install git
像上面的命令，有很多Linux会友好地告诉你Git没有安装，还会告诉你如何安装Git。
如果你碰巧用Debian或Ubuntu Linux，通过一条sudo apt-get install git就可以直接完成Git的安装，
非常简单。

5.这里输入的密码是不显示的,包括以后在终端执行sudo命令,这是linux安全策略。
你确信输入的密码正确,回车即可。

6.markdown编辑法：    加粗：两个*         斜体：一个*         斜体加粗：三个*          删除线：两个~

记住：所有的符号一律用“ 英文”形式下的！！！

7.疑问：在有道云里面怎么打出图片地址，网上图片的地址怎么复制？？？？

8.制作markdown表格时第二行的格式为-----|------|------，以此来形成表格形式，勿漏！！！ 

9.有道云笔记的Markdown还能支持制作待办事项，书写流程图、序列图、甘特图，书写数学公式等，
是不是够极客，够有逼格？   

10.如果你使用Windows系统，为了避免遇到各种莫名其妙的问题，请确保目录名（包括父目录）不包含中文。？？？

11.Microsoft的Word格式是二进制格式，因此，版本控制系统是没法跟踪Word文件的改动的，
前面我们举的例子只是为了演示，如果要真正使用版本控制系统，就要以纯文本方式编写文件。  
（只能跟踪“文本”文件的改动，比如TXT文件，网页，所有的程序代码等等！！！）word为二进制格式！！！

12.所以，创建一个版本库非常简单，首先，选择一个合适的地方，创建一个空目录：
$ mkdir learngit
$ cd learngit
$ pwd
/Users/michael/learngit
初始化一个Git仓库，使用git init命令。（变得可管理）
添加文件到Git仓库，分两步：
（1）使用命令git add <file>，注意，可反复多次使用，添加多个文件；
（2）使用命令git commit -m <message>，完成

13。要随时掌握工作区的状态，使用git status命令。
如果git status告诉你有文件被修改过，用git diff可以查看修改内容。
git log可以查看历史记录，改了什么内容，  git reset 可以回退到上一个版本。
git reflog在关机后仍然可以查看历史命令！！！
